public class Enums {
    public enum StatusTransaksi {
        PENDING,
        ONGOING,
        DONE,
        CANCELED
    }
    
    public static enum StatusProduk {
        AVAILABLE,
        RECOVERY,
        UNAVAILABLE
    }
    
    public static enum Role {
        ADMIN,
        CUSTOMER
    }
}
